import React from "react";

const Error = () => {
    // Renderiza o componente de erro.
    return (
        <div className="main" >
			<h1>Error</h1> {/* Exibe um título indicando que ocorreu um erro */}
        </div>
    );
};

// Exporta o componente para que possa ser utilizado em outras partes da aplicação.
export default Error;
