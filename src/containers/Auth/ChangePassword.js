import React from "react"; // Importa a biblioteca React, essencial para criar componentes funcionais.

const ChangePassword = () => { 
    // Define um componente funcional chamado "ChangePassword". 
    // Não recebe props e renderiza um conteúdo fixo.
    
    return (
        // Retorna o JSX que será renderizado na interface do usuário.
        <div className="main" >
			<h1>ChangePassword</h1>
        </div>
    );
};

export default ChangePassword; 
// Exporta o componente para que ele possa ser usado em outras partes da aplicação.
