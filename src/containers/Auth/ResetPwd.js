import React from "react";

const ResetPwd = () => {
    // Renderiza o componente de redefinição de senha.
    return (
        <div className="main" >
			<h1>ResetPwd</h1> {/* Exibe um título indicando que este é o componente de redefinição de senha */}
        </div>
    );
};

// Exporta o componente para que possa ser utilizado em outras partes da aplicação.
export default ResetPwd;
