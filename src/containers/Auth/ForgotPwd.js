import React from "react";

const ForgotPwd = () => {
    // Componente funcional que representa a página de recuperação de senha.
    return (
        <div className="main" >
            <h1>ForgotPwd</h1>
            // Renderiza um título principal informando ao usuário que esta é a página de recuperação de senha.
        </div>
    );
};

export default ForgotPwd;
// O componente é exportado como o padrão para ser utilizado em outras partes da aplicação.
