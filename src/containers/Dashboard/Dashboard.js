import React from "react";

const Dashboard = () => {
    // Renderiza o componente do painel de controle.
    return (
        <div className="main" >
			<h1>Dashboard</h1> {/* Exibe um título indicando que este é o componente do painel de controle */}
        </div>
    );
};

// Exporta o componente para que possa ser utilizado em outras partes da aplicação.
export default Dashboard;
